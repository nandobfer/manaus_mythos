// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://boz:Ewhblt69%21%40%23@agencyboz.com:3306/manaus_mythos"
}

model player {
    id          String          @id
    name        String
    actions     Int             @default(50)
    location    String          @default("Manaus")
}